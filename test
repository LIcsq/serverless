1. Generate Project:

syndicate generate project --name task04
cd task04


2. Generate Config:



export SDCT_CONF="/home/sweet/Desktop/deep-dive-into-serverless/task04/.syndicate-config-dev"


3. Generate Lambda:

Resources Names
Please note it is obligatory to stick to the following resources naming in order to pass the task:
- Lambda Function 1: sqs_handler
- SQS Queue: async_queue
- Lambda Function 2: sns_handler
- SNS Topic: lambda_topic

syndicate generate lambda --name sqs_handler --runtime python
syndicate generate lambda --name sns_handler --runtime python

syndicate generate meta sqs_queue \
    --resource_name async_queue \
    --fifo_queue false \
    --visibility_timeout 100 \
    --delay_seconds 0 \
    --maximum_message_size 1024 \
    --message_retention_period 60 \
    --receive_message_wait_time_seconds 20 

syndicate generate meta sns_topic \
    --resource_name lambda_topic \
    --region eu-central-1

AWSLambdaSQSQueueExecutionRole

{
    "resource_type" : "sqs_trigger",
    "target_queue" : "async_queue",
    "batch_size" : 100,
    "batch_window": 1
}   

{
    "resource_type": "sns_topic_trigger",
    "target_topic": "lambda_topic",
    "region": "string"|[]
}



